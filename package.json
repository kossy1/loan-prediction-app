{
  "scripts": {
    "vercel-build": "pip install --upgrade pip && pip install -r requirements.txt --no-cache-dir --timeout=1000 || pip install --no-cache-dir --timeout=1000 -r requirements.txt"
  }
}